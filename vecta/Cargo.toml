[package]
name = "vecta"
version = "0.1.0"
edition = "2021"
description = "A production-grade vector database built from scratch in Rust, benchmarked against FAISS"

[lib]
name = "vecta"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
rand = "0.8"

[build-dependencies]
pyo3-build-config = "0.29"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
